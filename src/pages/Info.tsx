import { Box, Button, Container } from '@chakra-ui/react';

function Info() {
  return (
    <Box w="full">
      <Container maxWidth="4xl">
        <Button>gen</Button>
      </Container>
    </Box>
  );
}

export default Info;
